let generatedCode = "", isRecording = false, recognition = null; if ("webkitSpeechRecognition" in window || "SpeechRecognition" in window) { const e = window.SpeechRecognition || window.webkitSpeechRecognition; recognition = new e, recognition.continuous = !0, recognition.interimResults = !1, recognition.lang = "en-US", recognition.onresult = function (e) { let t = ""; for (let o = e.resultIndex; o < e.results.length; o++)e.results[o].isFinal && (t += e.results[o][0].transcript + " "); "" !== t.trim() && (document.getElementById("prompt").value += t) }, recognition.onerror = function (e) { "no-speech" !== e.error && showToast("Mic error: " + e.error), stopRecording() }, recognition.onend = function () { isRecording && recognition.start() } } function toggleRecording() { isRecording ? stopRecording() : startRecording() } function startRecording() { if (isRecording) return; isRecording = !0, document.getElementById("mic-button").classList.add("recording"), document.getElementById("mic-icon").className = "fas fa-stop", document.getElementById("recording-indicator").classList.add("active"), document.getElementById("wave").classList.replace("wave-inactive", "wave-active"), recognition && recognition.start(), showToast("Recording started") } function stopRecording() { isRecording && (isRecording = !1, document.getElementById("mic-button").classList.remove("recording"), document.getElementById("mic-icon").className = "fas fa-microphone", document.getElementById("recording-indicator").classList.remove("active"), document.getElementById("wave").classList.replace("wave-active", "wave-inactive"), recognition && recognition.stop(), showToast("Recording stopped")) } function clearPrompt() { document.getElementById("prompt").value = "", document.getElementById("output").textContent = "", showToast("Cleared") } function copyCode() { const e = document.getElementById("output").textContent; e && (navigator.clipboard.writeText(e), showToast("Copied")) } function showToast(e) { const t = document.getElementById("toast"); t.textContent = e, t.classList.add("show"), setTimeout(() => t.classList.remove("show"), 2000) } async function generateCode() { const e = document.getElementById("prompt").value.trim(); if (!e) return void showToast("Enter prompt"); const t = document.getElementById("generate-btn"); t.classList.add("generating"), document.getElementById("output").textContent = "Generating..."; try { const o = await fetch("/generate-code", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ prompt: e }) }); if (!o.ok) throw new Error; const n = await o.json(); generatedCode = n.code || "", document.getElementById("output").textContent = generatedCode, showToast("Generated") } catch { document.getElementById("output").textContent = "Error", showToast("Failed") } finally { t.classList.remove("generating") } }